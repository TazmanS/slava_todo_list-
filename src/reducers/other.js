// import {LOGIN_USER} from '../actions/actionTypes'

const initialState = {
    otherCrash: [
        {
            'Имя' : 'ABK',
            "Название поломки": 'сломалось что-то'
        }
    ]
}

const other = (state = initialState, action) => {

switch(action.type){

    default: return state
}

}

export default other